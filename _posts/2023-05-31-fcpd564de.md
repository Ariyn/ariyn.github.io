---
layout: post
title: "RSA에 대하여"
date: "2023-05-31T22:38:21+07:00"
lastmod: "2023-06-11T21:47:27+07:00"
slug: "fcpd564de"
permalink: "/posts/fcpd564de/"
kind: dev
section: tech-blog
category: problem-analysis
tags:
  - "TIL"
  - "published"
  - "Area/스터디/RSA"
---
* Ron Rivest, Adi Shamir, Leonard Adleman에 의해서 만들어졌으며, 이름의 앞글자를 따서 RSA가 되었다 ([c:BLUE](/tags/c:BLUE)**R**ivest, [c:BLUE](/tags/c:BLUE)**S**hamir, [c:BLUE](/tags/c:BLUE)**A**dleman)
    * https://dl.acm.org/doi/pdf/10.1145/359340.359342
  * RSA의 제안 이유
    * 당시에 NBS (National Bureau of Standards)에서 Data Encryption Standard라는 표준을 제정함
    * 그러나 이 방식은 비밀키 방식이라 키 교환에 안전한 통신 채널이 필요했음
      * 비밀키 방식 = 비밀키 한 개로 암호화와 복호화를 진행함.
      * 따라서 전화선을 감청해서 비밀키를 알아낸다면, 이후의 모든 암호화를 깰 수 있다.
    * 따라서 "안전하지 않은 통신 채널에서 비밀키를 교환하는 방법"을 찾기 위해 RSA가 제안됨
      * 공개키 방식을 사용하면 모든 대화 내용을 감청해도 복호화가 불가능하다
  * 공개키 암호화 방식으로 공개키와 개인키 두 개의 키를 사용한다
    * 공개키 (public key)는 모두가 알고있고 모든 사람들이 메시지를 암호화하는데 사용된다.
      * 공개키로 암호화된 메시지는 개인키(private key)을 사용해야만 합리적인 시간 내에 복호화가 가능하다
    * 개인키는 혼자만 알고 있고, 공개키로 암호화된 메시지를 복호화하는 데 사용된다.
    * 같은 수학적 구조를 이용하면 전자서명도 만들 수 있다.
      * 서명은 "개인키로 암호화한다"라고 단순히 말하기보다, 메시지의 해시와 패딩을 개인키 연산으로 서명하고 공개키로 검증한다고 이해하는 편이 안전하다.
      * 다른 사람은 개인키를 볼 수 없기 때문에, 공개키로 검증되는 서명은 개인키 보유자가 만들었다고 볼 수 있다.
  * 공개키 방식에서 키 교환이 안전한 이유
    * 양쪽의 A, B는 자신의 공개키를 교환한다.
    * A가 메시지를 B의 공개키로 암호화한다. 이 암호문을 `c_b`라고 하자.
      * B는 `c_b`를 받고 자신의 개인키로 복호화해서 메시지를 읽는다.
    * 다시 B가 메시지를 A의 공개키로 암호화한다. 이 암호문을 `c_a`라고 하자.
      * A는 `c_a`를 받고 자신의 개인키로 복호화해서 메시지를 읽는다.
    * 이 과정에서 A의 공개키, B의 공개키, `c_b`, `c_a`가 모두 노출되어도 공격자는 개인키가 없기 때문에 원문을 읽기 어렵다.
      * 공개키는 암호화와 검증에 쓰이며, 같은 공개키로 자신이 만든 암호문을 복호화할 수 없다.
  * 결정론적 튜링머신으로 큰 숫자를 소인수 분해하기 어렵다는 사실에 기반하고 있다
    * 쇼어 알고리즘을 통해 양자 컴퓨터에서는 다항시간 내에 소인수 분해가 가능한 것이 증명되었다
    * 따라서 이론상 양자 컴퓨터의 상용화와 함께 RSA 암호의 수명이 끝나게된다
    * 하지만 양자컴퓨터의 도입이 멀었기 때문에 아직까지는 안전한 암호로 취급된다
  * 수학적 개념
    * 모듈러 연산을 사용하면 모든 정수 __m__ (0 <= __m__ < __n__)에 대해 매우 큰 세 개의 양의 정수 __e__, __d__, __n__을 구하는 것이 실용적이라는 사실에 기반한다.
    * $$(m^e)^d \equiv m \pmod {n}$$
      * $$\equiv$$는 모듈러 동치라는 뜻이다. 이는 $$(m^e)^d$$를 __n__으로 나눈것이나 __m__을 __n__으로 나눈것이나 나머지가 같다는 뜻이다.
        * C언어 등의 나머지 연산(%)를 사용하자면, 다음과 같다. $$(m^e)^d \% n == m \% n$$
      * $$(m^d)^e \equiv m \pmod {n}$$
        * __d__와 __e__의 순서가 바뀌어도 동일하다.
      * 이때 __e__, __n__ 심지어 __m__을 알더라도 __d__를 구하는 것은 매우 어려울 수 있다.
    * 여기서 공개키는 $$(n, e)$$이고, 개인키는 $$(n, d)$$라고 할 수 있다.
      * 공개키 암호화에서는 메시지 __m__을 $$c = m^e \pmod n$$으로 암호화한다.
      * 개인키 복호화에서는 암호문 __c__를 $$m = c^d \pmod n$$으로 되돌린다.
      * 서명에서는 방향이 반대처럼 보이지만, 실제 프로토콜에서는 원문 자체가 아니라 해시와 패딩이 들어간 값을 개인키 연산으로 서명한다.
  * 키 생성
    * 두 개의 큰 소수 __p__와 __q__를 선택한다.
      * __p__와 __q__는 무작위로 선택되어야 하며, 둘 다 크고 차이도 커야 한다.
      * 당연히 두 소수는 비밀로 유지되어야 한다
    * $$n=p*q$$을 계산한다
    * $$\phi(n)=(p-1)*(q-1)$$을 계산한다
    * 공개키로 사용할 __e__를 고른다
      * __e__는 $$\phi(n)$$과 서로소여야 한다.
      * 실제 구현에서는 보통 65537 같은 값을 많이 사용한다.
    * 개인키로 사용할 __d__를 $$e * d \equiv 1 \pmod {\phi(n)}$$에 따라 구한다
      * 즉 __d__는 __e__의 모듈러 곱셈 역원이다.
    * 공개키 암호화 -> 개인키 복호화
      * message = 15
      * 15^e mod n = 15^17 mod 2773 = 1392
      * 1392^d mod n = 1392^157 mod 2773 = 15
    * 공개키 암호화 -> 공개키로 다시 계산
      * message = 16
      * 16 ^ e mod n = 16 ^ 17 mod 2773 = 729
      * 729 ^ e mod n = 729 ^ 17 mod 2773 = 1051
      * 공개키만 두 번 쓰면 원문으로 돌아오지 않는다.
    * 개인키 서명 연산 -> 공개키 검증
      * message = 17
      * 17 ^ d mod n = 17 ^ 157 mod 2773 = 400
      * 400 ^ e mod n = 400 ^ 17 mod 2773 = 17
      * 개인키 연산은 검증 방향의 공개키 연산과 짝을 이뤄야 의미가 있다.
  * 암호화와 복호화
    * 메시지 M을 숫자 m으로 변환한다. $$(m < n)$$
      * 이 방법은 어떤 방법이던 상관없다.
      * 텍스트를 암호화하는게 아니라, 논리적으로 "숫자"를 암호화하기 위함이다.
      * m에서 다시 M으로 만드는 방법이 존재해야 하며, 양쪽 모두 이 방법을 알고 있어야 한다.
        * 예) 알파벳을 ascii코드로 변환해서 한 글자씩 암호화/복하화 하는 방식
    * 공개키 암호화에서는 $$c = m^e \pmod {n}$$를 전송한다.
  * 복호화
    * 전달받은 $$c$$에 대해 개인키 __d__ 제곱을 계산한다.
    * 이는 $$c^d \equiv (m^e)^d \equiv m \pmod {n}$$과 동일한 값을 만들게된다
    * 실제 RSA 암호화에서는 원문을 그대로 제곱하지 않고 OAEP 같은 padding scheme을 같이 사용한다.
  * 참고자료 (도움이 된 순서로)
    * https://en.wikipedia.org/wiki/RSA_(cryptosystem)
    * https://dl.acm.org/doi/10.1145/359340.359342
    * https://freshrimpsushi.github.io/posts/proof-of-rsa-public-key-cryptosystem/
    * https://chocolate-life.tistory.com/4
    * https://dimenchoi.tistory.com/48
